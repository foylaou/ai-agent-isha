# 複製這個檔案為 .env（跟這份檔案同一層），兩個 lab 共用。
# 填入實際值，這個檔案不要提交進 git。

# 講師部署的 LiteLLM Proxy（統一管理 API Key、記錄每次呼叫、控管模型存取）
LITELLM_API_BASE=http://<講師給的位址>:4000
LITELLM_API_KEY=

# 講師當天公布的 MCP server 位址，記得結尾要有 /mcp
MCP_SERVER_URL=http://<講師給的位址>:8000/mcp

# 選用：覆蓋單一 lab 預設的模型名稱（要跟 LiteLLM 後台設定的名稱一致）
# MODEL_NAME=claude-sonnet-4-6
